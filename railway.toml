[build]
builder = "nixpacks"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["python311", "pip"]

[build.nixpacksPlan.phases.install]
cmds = ["pip install python-telegram-bot==21.3 httpx==0.27.0"]

[deploy]
startCommand = "python main.py"
restartPolicyType = "on_failure"
